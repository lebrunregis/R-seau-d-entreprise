@model ReseauEntreprise.Areas.Employee.Models.ViewModels.Profile.IsAdminForm
@{ Layout = null; }

@if (Model.IsAdmin)
{
    @Html.ActionLink( "Admin", "Index", "Home", new { area = "Admin" }, new
{
@class = "glyphicon glyphicon-exclamation-sign btn btn-danger"
})
}
